syntax = "proto3";

package grpc_dummy;

message Person {
    string name = 1;
    int32 age = 2;
    string email = 3;
}

message detail_request {
    string email = 1;
}
//defing the services
service Details {
    //within services there are methods
    rpc get_detail(detail_request) returns (Person);
}
